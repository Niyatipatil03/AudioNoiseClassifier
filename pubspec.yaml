name: audio_noise_classifier
description: Offline AI Noise Classifier.
publish_to: 'none'
version: 1.0.0+1
environment:
    sdk: '>=3.0.0 <4.0.0'
dependencies:
    flutter:
         sdk: flutter
      # Library to run AI on phone
onnxruntime_flutter: ^0.1.0
record: ^6.0.0
path_provider: ^2.1.1
permission_handler: ^12.0.0
flutter:
    uses-material-design: true
assets:
    - assets/model.onnx
